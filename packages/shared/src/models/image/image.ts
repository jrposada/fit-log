export type Image = {
  /* Data */
  id: string;
  imageUrl: string;
  thumbnailUrl: string;
  imageWidth: number;
  imageHeight: number;

  /* Timestamps */
  createdAt: string;
  updatedAt: string;
};
